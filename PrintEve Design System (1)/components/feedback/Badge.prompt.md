**Badge** — compact pill for statuses, categories and metadata.

```jsx
<Badge variant="primary">Bestseller</Badge>
<Badge variant="success" dot>In production</Badge>
<Badge variant="outline">24h turnaround</Badge>
```

Variants: `default`, `primary`, `secondary`, `accent`, `success`, `danger`, `warning`, `outline`.

**Avatar** — circular user image with initials fallback on a soft-green chip.

```jsx
<Avatar name="Aditi Sharma" />
<Avatar src="/uploads/rohan.jpg" name="Rohan Mehta" size="lg" />
```

Sizes: `sm`, `md` (default), `lg`.
